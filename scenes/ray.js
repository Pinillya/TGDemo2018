class RayScene {
  constructor (renderer, camera) {
    // this.gl = document.getElementById('rayScene').getContext('webgl');
    // this.programInfo = twgl.createProgramInfo(this.gl, ["vs", "fs"]);
    // this.camera = camera;

    // console.log(this.gl);

    // var arrays = {
    //   position: [-1, -1, 0, 1, -1, 0, -1, 1, 0, -1, 1, 0, 1, -1, 0, 1, 1, 0],
    // };

    // this.bufferInfo = twgl.createBufferInfoFromArrays(this.gl, arrays);
    // var raytime = Date.now();
  }

  animate() {
    // var gl = this.gl;
    // var programInfo = this.programInfo;

    // var bps = 0.46153846153846153846153846153846;
    // var time =  (Date.now() - this.raytime)  * 0.001 * 2.0 * Math.PI * bps;
    // time -= 0.5+0.8*Math.sin(time);

    // const uniforms = {
    //   iTime: time,
    //   resolution: [gl.canvas.width, gl.canvas.height],
    //   camera: [8,5,7],
    // };

    // gl.useProgram(programInfo.program);
    // twgl.setBuffersAndAttributes(gl, programInfo, this.bufferInfo);
    // twgl.setUniforms(programInfo, uniforms); 
    // twgl.drawBufferInfo(gl, this.bufferInfo);

  }
}
